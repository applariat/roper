# Example usage

```
cat ../tests/test.json | go run main.go -f -

cat ../tests/test.json | go run main.go -f ../tests/test.yaml

go run main.go --file https://raw.githubusercontent.com/applariat/roper/master/tests/test.yaml
```